@model IEnumerable<Realestate_portal.Models.Tb_Status>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_editor.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Stage_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Stage_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Status }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Status }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Status })
        </td>
    </tr>
}

</table>
